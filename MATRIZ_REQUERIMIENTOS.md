# 📋 Matriz de Requerimientos Completa — Proyecto PCMATCH

| ID | Tipo | Categoría | Requisito | Descripción | Reglas de negocio | Validaciones de campos | Origen | Prioridad | Módulo / Archivo fuente | Criterio de aceptación | Estado actual |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| RF-001 | Funcional | — | Registro de Usuario | Permite a usuarios nuevos registrarse con nombre, correo, contraseña y datos personales opcionales. Se asigna automáticamente el rol `cliente`. | - El correo se almacena tal cual se recibe (no se normaliza a lowercase en register, solo en login). - La contraseña se hashea con `PASSWORD_BCRYPT` (no `Hash::make`). - El rol se fija como `'cliente'` y no puede ser especificado por el usuario. - Se genera token Sanctum inmediatamente tras el registro. - Se registra en historial de auditoría con `AuditLog::log`. | - `nombre` → string, obligatorio, max 255. - `correo` → email, obligatorio, único en tabla `usuarios`. - `password` → string, obligatorio, min 8 caracteres. - `apellido` → string, opcional, default `''`. - `telefono` → string, opcional, default `''`. | Ambos | Alta | `AuthController::register`, `Usuario` model, `api.php` (POST `/auth/register`) | Se crea usuario con rol cliente, se retorna token y datos del usuario. Correo duplicado retorna error 400. | Implementado |
| RF-002 | Funcional | — | Inicio de Sesión Multi-modelo | Autentica usuarios, bodegas y proveedores buscando secuencialmente en las 3 tablas (`usuarios`, `bodegas`, `proveedores`). | - Búsqueda secuencial: primero `usuarios`, luego `bodegas`, luego `proveedores`. - El correo se normaliza a minúsculas con `strtolower()` y se busca con `LOWER()`. - Verifica contraseña con `password_verify()`. - Si el usuario está inactivo (`activo=false`) retorna 403. - Si la bodega está inactiva (`activa=false`) retorna 403. - Si el proveedor está inactivo (`activo=false`) retorna 403. - Si el proveedor no está `aprobado` en `estado_aprobacion`, retorna 403 con estado. - Si el usuario tiene `perfil_id`, carga permisos del perfil activo. - Token Sanctum se nombra según tipo de entidad (`auth_token_usuario`, `auth_token_bodega`, `auth_token_proveedor`). | - `correo` → email, obligatorio. - `password` → string, obligatorio. | Ambos | Alta | `AuthController::login`, `Usuario`, `Bodega`, `Proveedor` models, `api.php` (POST `/auth/login`) | Retorna token Bearer y datos del usuario incluyendo rol y permisos. Credenciales incorrectas retornan 401. | Implementado |
| RF-003 | Funcional | — | Cierre de Sesión | Revocación del token Sanctum activo del usuario. | - El documento base indica que existe un endpoint `logout` que revoca tokens. | N/A | Documento base | Media | `AuthController` (endpoint `logout` referido en doc base) | Elimina token actual e impide peticiones autenticadas con dicho token. | Pendiente |
| RF-004 | Funcional | — | Solicitud de Recuperación de Contraseña | Genera un token temporal y envía enlace de restablecimiento por correo electrónico. | - Busca correo secuencialmente en `usuarios`, `bodegas` y `proveedores` con `LOWER()`. - Si el correo no existe, retorna éxito genérico (no revela si el email existe — seguridad). - Elimina tokens anteriores para el mismo email+tipo. - Token generado con `Str::random(64)` y almacenado hasheado con `Hash::make()`. - URL de reset construida con `FRONTEND_URL` del `.env`. - Si falla el envío de correo, se loguea error pero no se expone al usuario. | - `correo` → email, obligatorio. | Ambos | Alta | `PasswordResetController::sendResetLink`, `ResetPasswordMail`, `api.php` (POST `/auth/forgot-password`) | Se genera token en `password_reset_tokens` con campo `tipo` (usuario/bodega/proveedor). Siempre retorna éxito por seguridad. | Implementado |
| RF-005 | Funcional | — | Restablecimiento de Contraseña | Actualiza la contraseña previa validación de token y expiración. | - Busca todos los tokens para el email y verifica con `Hash::check()`. - Token expira a los 60 minutos (`addMinutes(60)->isPast()`). - Actualiza contraseña en la tabla correspondiente según `tipo` (usuario/bodega/proveedor). - Contraseña hasheada con `password_hash(PASSWORD_BCRYPT)`. - Limpia token usado después de actualizar. | - `token` → string, obligatorio. - `email` → email, obligatorio. - `password` → string, obligatorio, min 8, confirmed (requiere `password_confirmation`). | Ambos | Alta | `PasswordResetController::resetPassword`, `api.php` (POST `/auth/reset-password`) | Valida token, verifica expiración de 60 min, actualiza contraseña y limpia token. | Implementado |
| RF-006 | Funcional | — | Edición de Perfil Propio | Permite al usuario autenticado (de cualquier tipo) modificar sus datos personales y contraseña. | - Detecta tipo de modelo autenticado (`Bodega`, `Proveedor`, `Usuario`) y aplica reglas diferentes. - Para cambiar contraseña se requiere `password_actual` y se verifica con `password_verify()`. - Proveedor solo puede editar nombre y correo desde perfil. - Registra cambios detallados en auditoría con `AuditLog::log`. - Retorna permisos actualizados si el usuario tiene `perfil_id`. | - Usuario: `nombre` → obligatorio, max 100; `apellido` → opcional, max 100; `correo` → obligatorio, email, max 150, único excluyendo propio; `telefono` → opcional, max 20; `password` → opcional, min 8. - Bodega: `nombre` → obligatorio, max 150; `correo` → email, max 150, único en bodegas; `telefono` → opcional, max 20; `password` → opcional, min 8. - Proveedor: `nombre` → obligatorio, max 100; `correo` → email, max 100, único en proveedores; `password` → opcional, min 8. | Ambos | Media | `AuthController::profile`, `AuthController::updateProfile`, `api.php` (GET/PUT `/auth/profile`) | Actualiza datos del perfil y retorna datos actualizados con permisos. Si cambia contraseña sin enviar la actual, retorna error 422. | Implementado |
| RF-007 | Funcional | — | Gestión de Usuarios (CRUD) | Permite a Admin/Superadmin listar, crear, editar y eliminar usuarios del sistema. | - `index`: Requiere rol admin/superadmin + permiso `usuarios.ver`. Retorna usuarios con perfil_nombre y ordenados por created_at DESC. - `store`: Solo superadmin puede crear usuarios con rol `superadmin`. Rol por defecto es `cliente`. Permite asignar `perfil_id`. - `update`: Requiere permiso `usuarios.editar`. Cambiar `perfil_id` requiere `perfiles.asignar`. Solo superadmin puede asignar rol `superadmin`. - `destroy`: Eliminación física del usuario (no soft delete). Requiere permiso `usuarios.eliminar`. - Todas las acciones generan entrada de auditoría. | - Crear: `nombre` → obligatorio, max 255; `correo` → obligatorio, email, único; `password` → obligatorio, min 8; `rol` → opcional, in: admin,cliente. - Editar: `nombre` → obligatorio, max 255; `correo` → obligatorio, email, único excluyendo propio; `rol` → obligatorio, in: superadmin,admin,cliente; `activo` → opcional, boolean. - Eliminar: `id` → obligatorio (path param o query). | Ambos | Alta | `UsuarioController` (index, store, update, destroy), `api.php` (GET/POST/PUT/DELETE `/usuarios`) | CRUD completo con verificación de roles/permisos. Superadmin tiene acceso total, admin depende de permisos asignados. | Implementado |
| RF-008 | Funcional | — | Control de Acceso Basado en Roles (RBAC) | Restricción de rutas y operaciones según roles (cliente, admin, superadmin, bodega, proveedor). | - Frontend: `router.beforeEach` verifica `requiresAuth` y `roles` en meta. Redirige a panel correspondiente si rol no coincide. - Backend: Cada controlador verifica rol del usuario autenticado. - `Usuario::hasPermission()`: superadmin siempre retorna true; admin sin perfil_id retorna true (acceso total); admin con perfil_id verifica contra `perfil_permisos`. - Sanctum provider configurado para soportar autenticación multi-modelo (Usuario, Bodega, Proveedor). | N/A | Ambos | Alta | `Frontend/src/router/index.js`, `Usuario::hasPermission`, `PerfilController::checkAdmin`, todos los controllers | Usuarios sin rol autorizado son redirigidos. API retorna 403 si rol/permiso insuficiente. | Implementado |
| RF-009 | Funcional | — | Gestión de Perfiles y Permisos | CRUD de perfiles de permisos con asignación a usuarios y catálogo de permisos granulares por módulo. | - 8 módulos de permisos: Usuarios, Proveedores, Bodegas, Componentes, Cotizaciones, Perfiles, Historial, Reportes. - 24 permisos individuales definidos en `PERMISOS_DISPONIBLES`. - Al crear/editar perfil se valida que los permisos enviados existan en el catálogo. - Al eliminar perfil, se desasigna (`perfil_id = null`) de todos los usuarios. - `CASCADE` en FK elimina `perfil_permisos` al eliminar perfil. - Asignar perfil a usuario: endpoint separado `PUT /perfiles/asignar`. Permite enviar `perfil_id = null` para remover perfil. | - Crear: `nombre` → obligatorio, max 100, único; `descripcion` → opcional, max 255; `permisos` → obligatorio, array, min 1 elemento; `permisos.*` → string, max 100. - Editar: mismos campos + `activo` → opcional, boolean; `id` → obligatorio. - Asignar: `usuario_id` → obligatorio, integer; `perfil_id` → opcional, integer (null para remover). | Ambos | Alta | `PerfilController` (available, index, store, update, destroy, assign), `Perfil` model, `PerfilPermiso` model, `api.php` | CRUD completo de perfiles con validación de permisos contra catálogo. Asignación/desasignación de perfiles a usuarios. | Implementado |
| RF-010 | Funcional | — | Catálogo Público de Productos Base | Consulta pública del catálogo global de productos sin autenticación, y CRUD protegido para Admin/Superadmin. | - `index` (público): Sin autenticación. Filtros por categoría y búsqueda con `ILIKE`. Ordenamiento por categoría y nombre. - `store`: Solo admin/superadmin. Requiere permiso `componentes.crear`. Sube imagen a Supabase storage. - `update`: Solo admin/superadmin. Requiere permiso `componentes.editar`. Reemplaza imagen vieja al subir nueva. - `destroy`: Solo admin/superadmin. Requiere permiso `componentes.eliminar`. Bloquea eliminación si hay componentes activos usando el producto. Limpia registros pivote `proveedor_producto_catalogo`. | - Crear: `nombre` → obligatorio, max 255; `categoria` → obligatorio, in: CPU,GPU,RAM,Storage,PSU,Motherboard,Cooler,Case; `especificacion` → opcional, max 1000; `nucleos` → opcional, integer, min 1; `hilos` → opcional, integer, min 1; `frecuencia_hz` → opcional, numeric, min 0; `enfoque_uso` → opcional, in: estudio,oficina,gaming,diseño; `gama` → opcional, in: alta,media,baja; `imagen` → opcional, image, mimes: jpeg,png,jpg,webp, max 2048 KB. | Ambos | Alta | `CatalogoController` (index, store, update, destroy), `ProductoCatalogo` model, `api.php` (GET `/catalogo`, POST/GET `/productos-catalogo`) | Catálogo público sin auth. CRUD protegido con validación de integridad referencial para eliminación. | Implementado |
| RF-011 | Funcional | — | CRUD de Componentes de Inventario | Gestión completa de instancias de componentes en inventario de bodegas, con soporte para creación desde maestro o personalizada. | - `store` con `master_component_id`: Crea componente desde producto del catálogo, copia specs del maestro. Verifica que bodega exista y esté activa. Descuenta stock del proveedor si aplica (`proveedor_producto_catalogo`). Bloquea si stock del proveedor insuficiente. Bloquea duplicados (mismo bodega_id + producto_id). Genera SKU único. - `store` sin maestro: Creación personalizada. Bodega auto-asigna su ID. Proveedor debe indicar bodega_id y que la bodega le pertenezca. Bloquea duplicados (bodega_id + producto_id + especificacion). - `update`: Verifica propiedad para bodega/proveedor. Convierte comas decimales a puntos. Registra cambios detallados en auditoría. - `destroy`: Borrado lógico (SoftDeletes). Bloquea si stock > 0 (solo componentes en bodega). Bloquea si tiene cotizaciones activas en últimos 7 días con `stock_restaurado = false`. Elimina imagen de Supabase storage. | - Crear desde maestro: `master_component_id` → integer, exists en productos_catalogo; `bodega_id` → opcional, integer, exists en bodegas; `precio` → obligatorio, numeric, min 0; `descuento_porcentaje` → opcional, numeric, 0-100; `descuento_activo` → opcional, boolean; `stock` → obligatorio, integer, min 0; `imagen` → opcional, image, mimes: jpeg,png,jpg,webp, max 2048. - Crear personalizado: `producto_id` → obligatorio, exists en productos_catalogo; `especificacion` → obligatorio, max 1000; `gama` → obligatorio, in: alta,media,baja; `enfoque_uso` → opcional, in: estudio,oficina,gaming,diseño; `nucleos`, `hilos` → opcional, integer, min 1; `frecuencia_hz` → opcional, numeric, min 0. - Editar: todos campos opcionales; `precio` → numeric, gt 0; `stock` → integer, min 0; `gama` → in: alta,media,baja. | Ambos | Alta | `ComponenteController` (store, update, destroy, indexAdmin, indexBodega, maestros, indexPublic), `Componente` model, `api.php` | CRUD completo con validación de propiedad, descuento de stock del proveedor, generación de SKU, borrado lógico y protección de integridad. | Implementado |
| RF-012 | Funcional | — | Especificaciones Técnicas Detalladas | Almacenamiento estructurado de specs técnicas por componente: núcleos, hilos, frecuencia, enfoque de uso y gama. | - Modelo `Componente` tiene campos: `nucleos` (int), `hilos` (int), `frecuencia_hz` (decimal), `enfoque_uso` (string), `gama` (string). - Scopes de filtrado: `porNucleos`, `porHilos`, `porFrecuencia` (rango min/max), `porEnfoque`, `porGama`. - Modelo `ProductoCatalogo` también almacena specs técnicas (migración de reestructuración). - Gama: alta, media, baja. Enfoque: estudio, oficina, gaming, diseño. | - `nucleos` → integer, nullable, min 1. - `hilos` → integer, nullable, min 1. - `frecuencia_hz` → decimal(8,2), nullable, min 0. - `enfoque_uso` → varchar(20), nullable, in: estudio,oficina,gaming,diseño. - `gama` → varchar(10), nullable, in: alta,media,baja. | Ambos | Alta | `Componente` model (scopes), `ProductoCatalogo` model, migraciones `2026_06_10_182719`, `2026_07_03_210000` | Campos técnicos almacenados y filtrados dinámicamente tanto en componentes como en catálogo. | Implementado |
| RF-013 | Funcional | — | Configurador Interactivo (Builder) | Interfaz visual paso a paso para selección y ensamble de piezas de PC por parte del usuario cliente. | - Guía selección secuencial de: CPU, GPU, RAM, Storage, Motherboard, PSU, Cooler, Case. - Consume endpoint público `/componentes/publico` para listar componentes disponibles. - Solo accesible para rol `cliente` (ruta protegida `/armar`). | N/A (frontend) | Ambos | Alta | `Frontend/src/views/BuilderView.vue`, `Frontend/src/composables/useBuilder.js` | Interfaz guiada paso a paso. Solo clientes autenticados pueden acceder. | Implementado |
| RF-014 | Funcional | — | Verificación de Compatibilidad de Hardware | Algoritmo de validación de compatibilidad física y eléctrica entre piezas seleccionadas. | - El documento base referencia `ComponenteController::validarCompatibilidad`. - No se encontró dicho método en el código fuente actual. | N/A | Documento base | Alta | `ComponenteController` (método `validarCompatibilidad` referido en doc base — no encontrado en código) | Verifica socket CPU-Motherboard, TDP total vs PSU y formato de gabinete. | Pendiente |
| RF-015 | Funcional | — | Cálculo de Consumo Energético (TDP) | Sumatoria automática de potencia consumida y recomendación de PSU mínima. | - El documento base referencia cálculo de TDP en `BuilderView.vue`. - La lógica específica de TDP no pudo verificarse en el código del backend. | N/A | Documento base | Alta | `Frontend/src/views/BuilderView.vue` (referido en doc base) | Muestra consumo estimado en Watts y alerta si PSU insuficiente. | No verificado |
| RF-016 | Funcional | — | Creación y Guardado de Cotizaciones | Permite a clientes guardar configuración ensamblada como cotización, descuenta stock, genera PDF y envía por email. | - Solo rol `cliente` puede crear cotizaciones. - Usa transacción DB con `lockForUpdate()` para evitar race conditions de stock. - Valida stock suficiente por componente antes de descontar. - Genera código único con formato `COT-{8 chars random}`. - Genera PDF con `DomPDF` (vista `pdf.cotizacion`). - Envía email con PDF adjunto vía `CotizacionEnviadaMail`. - Si falla el envío de correo, se loguea pero no bloquea la operación. - Campo `stock_restaurado` se inicializa en `'false'`. | - `items` → array, obligatorio, no vacío. - `items[].componente_id` → integer, obligatorio, debe existir en componentes. - `items[].cantidad` → integer, default 1. - `items[].precio` → numeric (precio_unitario). - `total` → numeric, default 0. - `perfil` → string, in: gaming,oficina,diseño,estudio; default: gaming. | Ambos | Alta | `CotizacionController::store`, `CotizacionEnviadaMail`, `api.php` (POST `/cotizaciones`) | Cotización creada con descuento de stock atómico, PDF generado y email enviado. | Implementado |
| RF-017 | Funcional | — | Historial de Cotizaciones del Usuario | Consulta y visualización paginada de cotizaciones previas. | - `cliente`: Solo ve sus propias cotizaciones (`usuario_id = user->id`). - `admin`/`superadmin`: Ve todas las cotizaciones con datos del usuario (nombre, apellido, correo). Admin requiere permiso `cotizaciones.ver`. - `proveedor`: Ve cotizaciones que incluyen componentes de bodegas del proveedor (join a través de componentes → bodegas → proveedor_id). - Paginación fija de 15 registros. Ordenadas por `created_at DESC`. - Incluye conteo de items (`total_items`). | N/A | Ambos | Media | `CotizacionController::index`, `api.php` (GET `/cotizaciones`) | Lista paginada con diferenciación por rol. | Implementado |
| RF-018 | Funcional | — | Eliminación de Cotizaciones | Permite eliminar cotizaciones almacenadas. | - Roles permitidos: cliente, admin, superadmin. - Cliente solo puede eliminar sus propias cotizaciones (`usuario_id !== user->id` → 403). - Admin requiere permiso `cotizaciones.eliminar`. - Eliminación en cascada dentro de transacción: primero `cotizacion_items`, luego `cotizaciones`. - No restaura stock al eliminar (el stock se descuenta al crear). | - `id` → obligatorio (path param o query). | Ambos | Baja | `CotizacionController::destroy`, `api.php` (DELETE `/cotizaciones`) | Eliminación en cascada con verificación de propiedad para clientes. | Implementado |
| RF-019 | Funcional | — | Exportación/Vista de Cotización y Demo | Vista formateada de cotizaciones con PDF y vista demo pública. | - PDF generado con DomPDF en `CotizacionController::store`. - Vista demo: `DemoQuoteView.vue` accesible sin autenticación en `/ejemplo-cotizacion`. - Vista de cotización: `QuoteView.vue` accesible solo para clientes autenticados en `/cotizacion`. | N/A | Ambos | Media | `Frontend/src/views/DemoQuoteView.vue`, `Frontend/src/views/QuoteView.vue`, `CotizacionController::store` (generación PDF) | PDF generado automáticamente al crear cotización. Vista demo accesible públicamente. | Implementado |
| RF-020 | Funcional | — | Gestión de Almacenes / Bodegas (CRUD) | Registro, edición, activación/desactivación y eliminación de bodegas. | - `index`: Admin/superadmin ven todas las bodegas con proveedor_nombre. Proveedor solo ve sus propias bodegas. Admin requiere permiso `bodegas.ver`. Incluye conteo de componentes (`total_componentes`). - `store`: Bodega no puede crear bodegas. Proveedor auto-asigna su ID. Valida que proveedor asignado esté activo. Admin requiere `bodegas.crear`. - `update`: Proveedor solo edita sus propias bodegas. Superadmin y Admin pueden reasignar proveedor. Valida proveedor activo al reasignar. Admin requiere `bodegas.editar`. - `destroy`: Bodega no puede eliminar. Admin/superadmin no pueden eliminar bodega con proveedor asignado. Proveedor solo elimina sus propias bodegas. Al eliminar: limpia componentes e ítems de cotización en cascada. Admin requiere `bodegas.eliminar`. | - Crear: `nombre` → obligatorio, max 255; `correo` → obligatorio, email, único en bodegas; `password` → obligatorio, min 8; `proveedor_id` → opcional, integer. - Editar: `id` → obligatorio; `nombre`, `telefono` → opcionales; `activa` → boolean opcional; `proveedor_id` → solo admin/superadmin. | Ambos | Alta | `BodegaController` (index, store, update, destroy), `Bodega` model, `api.php` | CRUD completo con reglas de propiedad diferenciadas por rol. | Implementado |
| RF-021 | Funcional | — | Control de Stock de Inventario (Ajuste +/-) | Ajuste incremental o decremental de existencias por componente. | - Roles permitidos: admin, superadmin, proveedor, bodega. - Bodega solo ajusta stock de sus propios componentes. - Proveedor solo ajusta stock de componentes en sus bodegas. - Operación `decrementar`: valida que stock actual >= cantidad. Si insuficiente, retorna 422 con stock actual. - Operación `incrementar`: suma sin validación de límite superior. - Registra en auditoría con signo (+/-) y nuevo stock. | - `id` → obligatorio, integer. - `cantidad` → obligatorio, integer, min 1. - `operacion` → obligatorio, in: incrementar, decrementar. | Ambos | Alta | `ComponenteController::adjustStock`, `api.php` (PATCH `/componentes/stock`) | Incrementa/decrementa stock con validación de suficiencia y propiedad. | Implementado |
| RF-022 | Funcional | — | Traslado de Inventario entre Bodegas | Transferencia y reasignación de stock entre bodegas distintas. | - El documento base referencia esta funcionalidad en `BodegaController`. - No se encontró un endpoint o método específico de traslado entre bodegas en el código fuente. | N/A | Documento base | Media | `BodegaController` (referido en doc base — método de traslado no encontrado en código) | Registra movimiento de stock descontando del origen e incrementando en destino. | Pendiente |
| RF-023 | Funcional | — | Gestión de Proveedores (CRUD) | Administración del directorio de proveedores con datos legales y flujo de aprobación. | - `index`: Admin/superadmin/bodega pueden consultar. Incluye conteo de bodegas. Soporta paginación (15 por página). Admin requiere permiso `proveedores.ver`. - `store`: Solo admin/superadmin. Requiere `proveedores.crear`. `estado_aprobacion` es opcional (default según BD). Sube documento de soporte a storage `public`. - `update`: Requiere `proveedores.editar`. Cambiar `estado_aprobacion` requiere permiso `proveedores.aprobar` o ser superadmin. - `destroy`: Requiere `proveedores.eliminar`. Antes de eliminar, desasocia todas las bodegas (`proveedor_id = null`). Eliminación física (no soft delete). | - Crear: `nombre` → obligatorio, max 255; `correo` → obligatorio, email, único en proveedores; `password` → obligatorio, min 8; `identificacion_legal` → obligatorio, formato RUT/NIT (`^\d{9}-\d$`), único; `razon_social` → obligatorio, max 255; `documento_soporte` → opcional, file, mimes: pdf,jpg,jpeg,png, max 5120 KB; `estado_aprobacion` → opcional, in: pendiente,aprobado,rechazado. - Editar: `nombre` → obligatorio, max 255; `activo` → optional, integer; `estado_aprobacion` → opcional, in: pendiente,aprobado,rechazado; `identificacion_legal` → opcional, formato RUT/NIT (`^\d{9}-\d$`), único ignorando ID actual; `razon_social` → opcional, max 255. | Ambos | Alta | `ProveedorController` (index, store, update, destroy), `Proveedor` model, `api.php` | CRUD completo con flujo de aprobación. Solo superadmin/usuarios con permiso pueden aprobar/rechazar. | Implementado |
| RF-024 | Funcional | — | Catálogo de Productos por Proveedor | Gestión del catálogo de productos que cada proveedor ofrece, con precios mayoristas y stock. | - `productos`: Proveedor puede ver su propio catálogo con `id=me`. Admin/superadmin/bodega pueden ver catálogo de cualquier proveedor. Admin requiere `proveedores.ver`. - `syncProductos` con `items`: Proveedor gestiona su propio catálogo. Admin/superadmin pueden gestionar catálogo de cualquier proveedor. Usa `syncWithoutDetaching()` para no eliminar existentes. - `syncProductos` sin `items` (legacy): Acepta array de IDs. Usa `sync()` (reemplaza todo). - `updateCatalogoItem`: Solo proveedores. Actualiza precio_mayorista, stock y descripcion_comercial. Verifica que producto esté en su catálogo. - `removeCatalogoItem`: Solo proveedores. Elimina producto de su catálogo. | - syncProductos items: `items` → array obligatorio; `items.*.producto_catalogo_id` → integer, exists en productos_catalogo; `items.*.precio_mayorista` → numeric, min 0; `items.*.stock` → integer, min 0; `items.*.descripcion_comercial` → opcional, max 500. - updateCatalogoItem: `producto_catalogo_id` → integer, obligatorio; `precio_mayorista` → numeric, obligatorio, min 0; `stock` → integer, obligatorio, min 0; `descripcion_comercial` → opcional, max 500. | Ambos | Media | `ProveedorController` (productos, syncProductos, updateCatalogoItem, removeCatalogoItem), `Proveedor` model (relación productosCatalogo), `api.php` | Gestión completa del catálogo proveedor-producto con precio mayorista, stock y descripción. | Implementado |
| RF-025 | Funcional | — | Asistente Conversacional (Chatbot IA) | Chat interactivo con IA (Gemini API) para consultar hardware y recibir recomendaciones. | - Usa Gemini API con modelos en fallback: `gemini-2.0-flash-lite` → `gemini-2.0-flash` → `gemini-3.5-flash-lite`. - System instruction define reglas: nunca inventar datos, usar solo herramientas `ver_inventario` y `build_pc`. - `ver_inventario`: Busca componentes activos con stock > 0 en la BD. Filtra por categoría (ILIKE), palabra_clave (LOWER LIKE), precio_maximo. Limita a 10 resultados por precio ascendente. Aplica descuento si `descuento_activo = true`. - `build_pc`: Usa `RecomendacionService::buildPcIdeal()`. Si presupuesto ≤ 0, asigna valores por defecto según desempeño. - Reintentos: máximo 3 veces. 429 (cuota) → espera 3s y cambia modelo. 404 → cambia modelo. Error de red → espera 2s. - Timeout de 30 segundos por request a Gemini. - Endpoint público (sin autenticación). | - `messages` → array, obligatorio. | Ambos | Media | `ChatbotController::chat` (buscarInventario), `RecomendacionService`, `api.php` (POST `/chat`) | Chat funcional con function calling a Gemini. Busca inventario real y arma PCs con datos reales de la BD. | Implementado |
| RF-026 | Funcional | — | Recomendador de PC Ideal por Presupuesto | Motor de recomendación que genera múltiples opciones de builds de PC según uso, desempeño y presupuesto. | - Genera hasta 3 opciones: Equilibrada (proporciones estándar), Rendimiento (prioriza componente clave), Ahorro (85% del presupuesto, gama inferior). - Proporciones de presupuesto por uso: Gaming (GPU 30%, CPU 22%), Diseño (CPU 28%, GPU 25%), Estudio (CPU 25%, RAM 15%), Oficina (CPU 25%, Storage 15%). GPU opcional para uso `oficina`. - Algoritmo greedy: busca mejor componente por categoría dentro de sub-presupuesto. Fallback de gama: alta → media → baja. Fallback de uso: con uso → sin uso. - Fase de mejora: si queda presupuesto, intenta upgradar componentes por prioridad. Gaming: GPU > CPU > RAM > Storage. - Categorías requeridas: CPU, GPU, RAM, Storage, Motherboard, PSU, Cooler, Case. - Si ninguna opción se genera, calcula costo mínimo y lanza excepción con presupuesto mínimo estimado. - Descuento aplicado: `precio * (1 - descuento_porcentaje/100)` si `descuento_activo`. - Componentes más vendidos: endpoint separado que agrupa por enfoque_uso. Si datos insuficientes, complementa por gama (alta > media > baja). - Endpoint público (sin autenticación). | - buildPcIdeal: `uso` → obligatorio, in: gaming,estudio,oficina,diseño; `desempeno` → obligatorio, in: alta,media,baja; `presupuesto_max` → obligatorio, numeric, min 1. - getMasVendidos: `limit` → opcional, integer, máximo 20, default 6. | Ambos | Alta | `RecomendacionController` (buildPcIdeal, getMasVendidos), `RecomendacionService`, `api.php` (POST `/recomendaciones/pc-ideal`, GET `/recomendaciones/mas-vendidos`) | Genera builds completas respetando presupuesto. Fallback progresivo de gama. Múltiples opciones diferenciadas. | Implementado |
| RF-027 | Funcional | — | Dashboard de Analíticas | Panel de analíticas con rotación de componentes por bodega y consumo por proveedor. | - `selectores`: Retorna listas de bodegas activas y proveedores activos para selectores del dashboard. - `rotacionBodega`: Muestra los componentes más cotizados en una bodega. Filtra por rango de fechas: 1_mes, 3_meses, histórico. Límite de resultados: configurable (default 10, máximo 50). Incluye nombre de producto, categoría y especificación. - `consumoProveedor`: Distribuye consumo de cotizaciones por bodegas del proveedor. Calcula porcentajes sobre total general. Incluye nombre de bodega. - Todos requieren rol admin/superadmin. Admin requiere permiso `reportes.ver`. | - rotacionBodega: `bodega_id` → obligatorio; `rango_fecha` → opcional, default 'historico'; `limit` → opcional, default 10, max 50. - consumoProveedor: `proveedor_id` → obligatorio. | Ambos | Alta | `AnaliticaController` (selectores, rotacionBodega, consumoProveedor), `api.php` (GET `/analiticas/*`) | Analíticas de rotación y consumo con filtros por periodo y límite configurable. | Implementado |
| RF-028 | Funcional | — | Auditoría de Acciones del Sistema | Registro automático de todas las operaciones CRUD del sistema con detalle de cambios. | - `AuditLog::log()`: Registra usuario_id, usuario_nombre, rol_usuario, accion (texto descriptivo) y modulo. Soporta los 3 tipos de modelo (Usuario, Bodega, Proveedor). - `AuditLog::formatChanges()`: Formatea cambios dirty del modelo con labels legibles. Resuelve FK a nombres (proveedor_id → nombre, bodega_id → nombre). Formatea precios con $, booleanos como Activo/Inactivo, roles con nombre completo. Nunca muestra contraseñas en texto. - `HistorialController::index`: Paginación de 15 registros, orden DESC. Solo admin/superadmin. Admin requiere permiso `historial.ver`. - Módulos registrados: Usuarios, Componentes, Bodegas, Proveedores, Perfiles, Cotizaciones, Catálogo, Perfil. | - `historial_acciones`: `usuario_id` → integer; `usuario_nombre` → string; `rol_usuario` → string; `accion` → string; `modulo` → string. | Ambos | Alta | `AuditLog` helper, `HistorialAccion` model, `HistorialController::index`, `api.php` (GET `/historial`) | Todas las operaciones CRUD generan registro de auditoría con detalle de cambios legibles. | Implementado |
| RF-029 | Funcional | — | Health Check de Infraestructura | Endpoint para verificar estado del servidor API y conectividad con la base de datos. | - Intenta conexión a BD con `DB::connection()->getPdo()`. - Si conecta: retorna `{status: 'ok', db: 'connected'}`. - Si falla: retorna `{status: 'ok', db: 'disconnected'}` (status 200 en ambos casos). - Endpoint público, sin autenticación. | N/A | Ambos | Media | `api.php` (GET `/health`) | Retorna JSON con estado de API y DB. Status 200 siempre. | Implementado |
| RF-030 | Funcional | — | Endpoint Sanctum `/auth/me` | Endpoint de prueba para verificar que el middleware Sanctum funciona correctamente. | - Retorna el usuario autenticado completo (vía `$request->user()`). - Requiere autenticación Sanctum. | N/A | Detectado en código | Baja | `api.php` (GET `/auth/me`) | Retorna datos del usuario autenticado como prueba de Sanctum. | Implementado |
| RF-031 | Funcional | — | Vista Pública de Componentes | Endpoint público para consultar componentes activos con stock disponible, sin autenticación. | - Solo muestra componentes activos, con stock > 0, en bodegas activas. - Filtros por categoría y búsqueda. - Incluye datos de descuento y precio_final. | N/A | Detectado en código | Alta | `ComponenteController::indexPublic`, `api.php` (GET `/componentes/publico`) | Lista pública de componentes disponibles para consulta sin login. | Implementado |
| RF-032 | Funcional | — | Consulta de Componentes Maestros | Endpoint para ver componentes maestros (sin bodega asignada) que pueden ser importados a inventario de bodegas. | - Filtra componentes donde `bodega_id IS NULL` y `activo = true`. - Soporta filtros por categoría y búsqueda. - Requiere autenticación (admin, bodega o proveedor). | N/A | Detectado en código | Media | `ComponenteController::maestros`, `api.php` (GET `/componentes/maestros`) | Lista de componentes maestros disponibles para importación a bodegas. | Implementado |
| RF-033 | Funcional | — | Generación Automática de SKU | Genera identificadores únicos para cada componente con formato `{CATEGORIA}-{PROD_ID}-{BODEGA_ID}-{HEX}`. | - Formato: `{CATEGORIA (mayúsculas)}-{producto_id pad 3}-{bodega_id pad 3}-{HEX 4 chars}`. - Garantiza unicidad incluyendo registros soft-deleted (`withTrashed()`). - Si SKU existe, regenera parte HEX hasta encontrar uno único. | - `sku` → varchar(50), NOT NULL, UNIQUE (constraint en BD). | Detectado en código | Alta | `Componente::generarSku`, migración `2026_05_25_250000` | SKU único generado automáticamente con formato consistente. | Implementado |
| RF-034 | Funcional | — | Sistema de Descuentos por Componente | Gestión de descuento porcentual por componente con activación/desactivación independiente. | - Cada componente tiene `descuento_porcentaje` (0-100) y `descuento_activo` (boolean). - `getPrecioFinalAttribute()`: calcula `precio * (1 - descuento_porcentaje / 100)` si descuento activo y porcentaje > 0. - Usado en recomendaciones, chatbot, vistas públicas y builder. | - `descuento_porcentaje` → decimal(5,2), nullable, default 0, min 0, max 100. - `descuento_activo` → boolean, default false. | Detectado en código | Media | `Componente` model (accessor `precio_final`), `ComponenteController::store/update`, migración `2026_07_03_210000` | Precio final calculado dinámicamente. Descuento visible en todas las vistas. | Implementado |
| RF-035 | Funcional | — | Envío de Cotización por Email con PDF | Genera PDF de cotización y lo envía adjunto al correo del cliente. | - PDF generado con DomPDF (vista `pdf.cotizacion`). - Email enviado via `CotizacionEnviadaMail` con subject `Tu Cotización en PCMATCH - {codigo}`. - PDF adjunto nombrado `Cotizacion-{codigo}.pdf`. - Si falla envío, se loguea error pero la cotización se crea exitosamente. | N/A | Detectado en código | Media | `CotizacionController::store`, `CotizacionEnviadaMail`, template `emails.cotizacion` | PDF generado y enviado como adjunto en email. Fallo de email no bloquea la operación. | Implementado |
| RF-036 | Funcional | — | Envío de Email de Reset de Contraseña | Email transaccional con enlace de restablecimiento de contraseña personalizado. | - Template: `emails.reset-password`. - Subject: `Restablecer contraseña — PCMATCH`. - Incluye URL de reset y nombre del usuario. - Fallo de envío se loguea pero no se reporta al usuario. | N/A | Detectado en código | Alta | `PasswordResetController::sendResetLink`, `ResetPasswordMail`, template `emails.reset-password` | Email enviado con enlace de reset. Fallo silencioso por seguridad. | Implementado |
| RF-037 | Funcional | — | Páginas Informativas Públicas | Vistas públicas de información: Sobre Nosotros, Contacto. | - `AboutView.vue`: accesible en `/sobre-nosotros`. - `ContactView.vue`: accesible en `/contacto`. - Sin autenticación requerida. | N/A | Detectado en código | Baja | `Frontend/src/views/AboutView.vue`, `Frontend/src/views/ContactView.vue`, `router/index.js` | Páginas informativas accesibles públicamente. | Implementado |
| RF-038 | Funcional | — | Panel de Administración (AdminView) | Panel completo para rol admin con gestión de todos los módulos según permisos asignados. | - Vista administrativa con pestañas para cada módulo (usuarios, bodegas, proveedores, componentes, cotizaciones, perfiles, historial, analíticas). - Accesible solo para rol `admin`. - Funcionalidades limitadas por permisos del perfil asignado. | N/A | Detectado en código | Alta | `Frontend/src/views/AdminView.vue`, `router/index.js` (meta: roles: ['admin']) | Panel admin con todas las secciones de gestión, filtrado por permisos. | Implementado |
| RF-039 | Funcional | — | Panel de Super Administración (SuperAdminView) | Panel completo para superadmin con acceso total a todos los módulos sin restricciones de permisos. | - Vista con acceso total a todos los módulos de administración. - Accesible solo para rol `superadmin`. - No requiere verificación de permisos individuales. | N/A | Detectado en código | Alta | `Frontend/src/views/SuperAdminView.vue`, `router/index.js` (meta: roles: ['superadmin']) | Panel superadmin con acceso irrestricto a todas las funcionalidades. | Implementado |
| RF-040 | Funcional | — | Panel de Bodega (BodegaView) | Panel para el rol bodega con gestión de inventario propio. | - Accesible solo para rol `bodega`. - Permite gestionar componentes propios, importar desde maestros, ajustar stock. | N/A | Detectado en código | Alta | `Frontend/src/views/BodegaView.vue`, `router/index.js` (meta: roles: ['bodega']) | Panel bodega con gestión de inventario propio. | Implementado |
| RF-041 | Funcional | — | Panel de Proveedor (ProveedorView) | Panel para el rol proveedor con gestión de catálogo, bodegas propias y componentes. | - Accesible solo para rol `proveedor`. - Permite gestionar catálogo de productos, bodegas propias y componentes en sus bodegas. | N/A | Detectado en código | Alta | `Frontend/src/views/ProveedorView.vue`, `router/index.js` (meta: roles: ['proveedor']) | Panel proveedor con gestión de catálogo y bodegas. | Implementado |
| RF-042 | Funcional | — | Panel de Cliente (ClientHomeView) | Vista principal para clientes autenticados con acceso a builder, cotizaciones y recomendaciones. | - Accesible solo para rol `cliente` en ruta `/inicio`. - Hub central del cliente con navegación a armar PC, cotizaciones e historial. | N/A | Detectado en código | Alta | `Frontend/src/views/ClientHomeView.vue`, `router/index.js` (meta: roles: ['cliente']) | Dashboard del cliente con acceso a todas las funcionalidades del cliente. | Implementado |
| RF-043 | Funcional | — | Componente Navbar Global | Barra de navegación adaptativa según rol del usuario autenticado. | - Muestra opciones de navegación diferenciadas por rol. - Integra toggle de tema (dark/light). - Incluye botón de logout. | N/A | Detectado en código | Media | `Frontend/src/components/Navbar.vue`, `Frontend/src/App.vue` | Navbar responsiva con navegación por rol. | Implementado |
| RF-044 | Funcional | — | Sistema de Notificaciones Toast | Componente de notificaciones visuales para feedback al usuario. | - Composable `useToast` para gestión de mensajes. - Componente `ToastContainer.vue` para renderizado visual. | N/A | Detectado en código | Baja | `Frontend/src/composables/useToast.js`, `Frontend/src/components/ToastContainer.vue` | Notificaciones toast funcionales en toda la aplicación. | Implementado |
| RF-045 | Funcional | — | Toggle de Tema (Dark/Light Mode) | Alternancia entre modo oscuro y claro en toda la interfaz. | - Composable `useTheme` gestiona el estado del tema. - Persiste preferencia del usuario. | N/A | Detectado en código | Baja | `Frontend/src/composables/useTheme.js` | Toggle funcional entre dark y light mode con persistencia. | Implementado |
| RF-046 | Funcional | — | Componente de Tarjeta de Componente | Tarjeta visual reutilizable para presentar componentes de hardware. | - Componente `ComponentCard.vue` para mostrar info resumida de componentes (nombre, precio, imagen, specs). | N/A | Detectado en código | Baja | `Frontend/src/components/ComponentCard.vue` | Tarjeta visual reutilizable de componente. | Implementado |
| RF-047 | Funcional | — | Protección de Integridad de Componentes en Eliminación | Bloqueo de eliminación de componentes con stock o cotizaciones activas recientes. | - No permite eliminar componente de bodega con stock > 0 (retorna 409 con mensaje y cantidad). - No permite eliminar si tiene cotizaciones en últimos 7 días con `stock_restaurado = false`. - Implementado en `Componente::tieneRelacionesActivas()`. | N/A | Detectado en código | Alta | `ComponenteController::destroy`, `Componente::tieneRelacionesActivas` | Eliminación bloqueada con mensajes específicos si hay stock o relaciones activas. | Implementado |
| RF-048 | Funcional | — | Índices de Rendimiento en BD | Índices de performance creados para optimizar consultas frecuentes. | - Índice en `componentes(bodega_id)`. - Índice en `componentes(producto_id)`. - Índice compuesto UNIQUE en `componentes(bodega_id, producto_id, especificacion)`. - Índice en `cotizacion_items(cotizacion_id)`. - Índice en `cotizacion_items(componente_id)`. | N/A | Detectado en código | Media | Migraciones `2026_06_12_200609`, `2026_05_25_250000` | Índices creados para optimización de JOINs y búsquedas frecuentes. | Implementado |
| RNF-001 | No Funcional | Seguridad | Autenticación Segura y Hashing | Contraseñas encriptadas con bcrypt, autenticación via Laravel Sanctum con tokens Bearer. | - Todas las contraseñas hasheadas con `password_hash(PASSWORD_BCRYPT)`. - Token Sanctum con nombres diferenciados por tipo de entidad. - Tokens almacenados en tabla `personal_access_tokens`. - Verificación de contraseña con `password_verify()`. | N/A | Ambos | Alta | `AuthController`, `UsuarioController`, `BodegaController`, `ProveedorController`, `PasswordResetController`, migración `personal_access_tokens` | Ninguna clave en texto plano. API requiere Bearer token. | Implementado |
| RNF-002 | No Funcional | Seguridad | Protección contra Inyección y XSS | Uso de ORM Eloquent con consultas parametrizadas. Validación estricta de entrada con Validator. | - Eloquent ORM previene SQL injection por defecto. - Validator de Laravel sanitiza y valida todos los inputs. - Uso de `whereRaw` con parámetros bindeados (`?`). - CORS configurado (middleware Laravel). | N/A | Ambos | Alta | Todos los controllers (uso de Validator, Eloquent), `backend-laravel/config/cors.php` | Consultas parametrizadas. Validación de inputs. CORS habilitado. | Implementado |
| RNF-003 | No Funcional | Rendimiento | Optimización de Consultas API | Selectores explícitos, JOINs eficientes e índices en BD para consultas rápidas. | - Selectores explícitos (`select(...)`) en todas las consultas de listado. - JOINs optimizados con `leftJoin` y `groupBy`. - Índices de rendimiento creados en migraciones. - Paginación en endpoints de alto volumen (cotizaciones: 15, historial: 15, proveedores: 15). | N/A | Ambos | Alta | Todos los controllers, migraciones de índices | Consultas optimizadas con selectores, joins e índices. Paginación implementada. | Implementado |
| RNF-004 | No Funcional | Rendimiento | Carga Rápida de SPA | Frontend Vue 3 + Vite con empaquetamiento modular. | - Vite como bundler con HMR. - Tailwind CSS para estilos eficientes. - Importaciones directas de componentes en router (sin lazy loading explícito). | N/A | Ambos | Media | `Frontend/vite.config.js`, `Frontend/src/router/index.js`, `Frontend/package.json` | Build optimizado con Vite. | Implementado |
| RNF-005 | No Funcional | Usabilidad | Diseño Responsivo y Moderno | Interfaz adaptativa con Tailwind CSS para todos los viewports. | - Tailwind CSS como framework de estilos utilitarios. - Estilos globales en `style.css`. - Configuración personalizada en `tailwind.config.js`. | N/A | Ambos | Alta | `Frontend/src/style.css`, `Frontend/tailwind.config.js`, todos los `.vue` files | Diseño responsivo con Tailwind CSS en todas las vistas. | Implementado |
| RNF-006 | No Funcional | Escalabilidad | Arquitectura Desacoplada Contenedorizada | Frontend SPA y Backend REST API separados, orquestados con Docker Compose. | - 3 servicios Docker: `app` (PHP), `webserver` (Nginx en puerto 8000), `frontend` (Vite en puerto 5173). - Volúmenes mapeados para desarrollo hot-reload. - Red interna `pcmatch_network`. - BD MySQL comentada (migrada a Supabase cloud). - Soporte para deploy en Render (Backend Dockerfile) y Vercel (Frontend SPA). | N/A | Ambos | Alta | `docker-compose.yml`, `docker/`, `backend-laravel/Dockerfile`, `Frontend/vercel.json`, `render.yaml` | Servicios independientes orquestados con Docker. Deploy a Render y Vercel soportado. | Implementado |
| RNF-007 | No Funcional | Disponibilidad | Persistencia con Supabase | BD PostgreSQL gestionada en la nube con Supabase. | - Conexión configurada via `.env` con credenciales de Supabase. - Storage de archivos (imágenes de componentes y documentos) en Supabase Storage. - Health check endpoint verifica conexión a BD. | N/A | Ambos | Alta | `backend-laravel/.env`, `backend-laravel/.env.render`, `api.php` (GET `/health`) | Conexión a Supabase PostgreSQL verificable via health check. Storage de archivos en cloud. | Implementado |
| RNF-008 | No Funcional | Mantenibilidad | Principios SOLID y Estructura MVC | Separación clara de responsabilidades: Controllers, Models, Services, Helpers, Composables. | - Controllers: 13 controladores API especializados por dominio. - Models: 9 modelos con relaciones, scopes y métodos de negocio. - Services: `RecomendacionService` encapsula lógica compleja. - Helpers: `AuditLog` centraliza logging. - Frontend: composables (`useAuth`, `useBuilder`, `useTheme`, `useToast`) para lógica reutilizable. | N/A | Ambos | Media | Toda la estructura de `backend-laravel/app/` y `Frontend/src/` | Separación de responsabilidades entre capas. Código modular y reutilizable. | Implementado |
| RNF-009 | No Funcional | Mantenibilidad | Documentación de Casos de Prueba QA | Suite completa de 10 archivos de casos de prueba en Markdown. | - Cubre: autenticación/perfil, gestión usuarios, perfiles/permisos, componentes/inventario, cotizaciones, bodegas, proveedores, builder/ensamble, asistente/recomendaciones, auditoría/analíticas. | N/A | Ambos | Media | `qa/casos-prueba/` (10 archivos .md) | 10 suites de prueba documentadas cubriendo todos los módulos. | Implementado |
| RNF-010 | No Funcional | Compatibilidad | Portabilidad Multi-plataforma | Ejecución consistente en Windows, Linux y macOS via Docker. | - Docker Compose define entorno reproducible. - Node 18+ para frontend, PHP 8.2+ para backend. - Dockerfiles para producción (Render). | N/A | Ambos | Baja | `docker-compose.yml`, Dockerfiles, `README.md` | Entorno reproducible con Docker independiente del SO. | Implementado |
| RNF-011 | No Funcional | Seguridad | No Exposición de Existencia de Email | En recuperación de contraseña, no se revela si un email existe o no en el sistema. | - `sendResetLink` siempre retorna `success: true` con mensaje genérico, independientemente de si el correo existe. | N/A | Detectado en código | Alta | `PasswordResetController::sendResetLink` | Respuesta genérica sin revelar existencia de cuentas. | Implementado |
| RNF-012 | No Funcional | Seguridad | Expiración de Tokens de Reset | Tokens de restablecimiento de contraseña expiran tras 60 minutos. | - Verificación con `Carbon::parse(created_at)->addMinutes(60)->isPast()`. - Token expirado se limpia de la BD. | N/A | Detectado en código | Alta | `PasswordResetController::resetPassword` | Tokens expiran automáticamente a los 60 minutos. | Implementado |
| RNF-013 | No Funcional | Disponibilidad | Fallback de Modelos de IA | Sistema de fallback entre múltiples modelos de Gemini para alta disponibilidad del chatbot. | - 3 modelos en orden de preferencia: `gemini-2.0-flash-lite`, `gemini-2.0-flash`, `gemini-3.5-flash-lite`. - Cambia modelo ante error 429 (cuota) o 404 (no disponible). - Máximo 3 reintentos con esperas de 2-3 segundos. | N/A | Detectado en código | Media | `ChatbotController::chat` | Chatbot resiliente con fallback entre modelos de IA. | Implementado |
| RNF-014 | No Funcional | Seguridad | Transacciones Atómicas para Cotizaciones | Uso de transacciones de BD con `lockForUpdate()` para prevenir race conditions en stock. | - `DB::transaction()` envuelve toda la creación de cotización. - `lockForUpdate()` en cada componente para evitar venta concurrente del mismo stock. - Si cualquier validación falla, toda la transacción se revierte. | N/A | Detectado en código | Alta | `CotizacionController::store` | Integridad de stock garantizada con transacciones y locks pesimistas. | Implementado |
| RNF-015 | No Funcional | Rendimiento | Filtros Dinámicos con Scopes | Scopes de Eloquent para filtrado eficiente de componentes. | - 10 scopes definidos: `activo`, `inactivo`, `conStock`, `porCategoria`, `porGama`, `rangoPrecio`, `buscar`, `porNucleos`, `porHilos`, `porFrecuencia`, `porEnfoque`. - Reutilizados en múltiples endpoints (indexAdmin, indexBodega, indexPublic). | N/A | Detectado en código | Media | `Componente` model (scopes) | Filtros componibles y reutilizables. | Implementado |

---

## Resumen

### Por Tipo
| Tipo | Cantidad |
| :--- | :--- |
| Requisitos Funcionales (RF) | 48 |
| Requisitos No Funcionales (RNF) | 15 |
| **Total General** | **63** |

### Por Prioridad
| Prioridad | Cantidad |
| :--- | :--- |
| Alta | 36 |
| Media | 18 |
| Baja | 9 |
| **Total General** | **63** |

### Por Estado
| Estado | Cantidad |
| :--- | :--- |
| Implementado | 60 |
| Pendiente | 2 |
| No verificado | 1 |
| **Total** | **63** |

### Por Origen
| Origen | Cantidad |
| :--- | :--- |
| Ambos (Documento base + Código) | 39 |
| Detectado en código | 22 |
| Documento base (solo) | 2 |
| **Total** | **63** |

### Brecha: Requisitos del Documento Base "Pendiente"
| ID | Requisito | Motivo |
| :--- | :--- | :--- |
| RF-003 | Cierre de Sesión (`logout`) | El documento base referencia un método `logout` en `AuthController`, pero no se encontró dicho método ni endpoint en el código fuente actual. |
| RF-014 | Verificación de Compatibilidad de Hardware | El documento base referencia un método `validarCompatibilidad` en `ComponenteController`, pero no existe dicho método en el código fuente actual. |
| RF-022 | Traslado de Inventario entre Bodegas | El documento base indica traslado de stock entre bodegas en `BodegaController`, pero no se encontró endpoint ni lógica de traslado en el código fuente actual. |

**Total de requisitos del documento base pendientes de implementación: 3 de 29 (10.3%)**
